apiVersion: apps/v1
kind: Deployment
metadata:
  name: python
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
        - name: python
          image: francesdbz18/pythonb10:latest
          ports:
            - containerPort: 8090
          envFrom:
            - secretRef:
                name: api-env
---
apiVersion: v1
kind: Service
metadata:
  name: python
spec:
  selector:
    app: python
  ports:
    - port: 8090
      targetPort: 8090
